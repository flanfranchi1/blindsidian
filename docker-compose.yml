services:
  notty:
    build: .
    ports:
      - "${HOST_PORT:-8080}:8080"
    environment:
      PORT: ":8080"
      STORAGE_PATH: "/data"
      SESSION_NAME: "${SESSION_NAME:-notty_session}"
    volumes:
      - ./storage:/data:Z,U
    restart: unless-stopped
